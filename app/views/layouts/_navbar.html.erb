<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
	<%= link_to 'Pizz App', root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExampleDefault">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
      	<%= link_to 'Home', root_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Link</a>
      </li>
    </ul>
    <ul class="navbar-nav dropleft">
    	<li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle ml-auto" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Dropdown</a>
        <div class="dropdown-menu" aria-labelledby="dropdown01">
        	<% if !current_user.blank? %>
        		<%= link_to current_user.email, "#", class: "dropdown-item" %>
        	<% end %>
        	<% if !current_user.blank? %>
						<%= link_to "New Pizza", new_pizza_path, class: "dropdown-item" %>
        	<% end %>
        	<%= current_user.blank? ? (link_to 'Login', new_user_session_path, class: "dropdown-item") : 
						(link_to 'Logout', destroy_user_session_path, method: :delete, class: "dropdown-item") %>
        </div>
      </li>
    </ul>
  </div>
</nav>